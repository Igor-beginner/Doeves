spring:
  datasource:
    username: test
    password: test

logging:
  level:
    org:
    springframework:
      jdbc:
        core:
          JdbcTemplate: DEBUG
          StatementCreatorUtils: TRACE
        datasource:
          DataSourceUtils: DEBUG
        support: DEBUG